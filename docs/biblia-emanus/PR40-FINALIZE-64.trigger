Finalize the already verified PR40 corpus.

Inputs are fixed:
- 61 previously green works from run 31110524525;
- DID, TEMP_SCR and ADD_PSA final green artifacts from run 31152967863.

Do not retranslate any work. Assemble exactly 64 works, require the atomic verdict to report 64 works and zero blockers, run the published-aware application validation, and commit only after all checks pass.
