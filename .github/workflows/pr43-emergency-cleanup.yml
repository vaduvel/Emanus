name: PR43 emergency cleanup (retired)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: windows-2022
    steps:
      - run: echo 'Emergency cleanup retired; publication authfix owns run cancellation.'
